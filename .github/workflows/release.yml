name: Release Obsidian plugin

on:
    push:
        tags:
            - '*'

jobs:
    build:
        runs-on: ubuntu-latest
        permissions:
            contents: write
            id-token: write
            attestations: write
        steps:
            - uses: actions/checkout@v6

            - name: Use Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: 24
                  cache: 'npm'

            - name: Build plugin
              run: |
                  npm ci
                  npm run build

            - name: Attest build provenance
              uses: actions/attest@v4
              with:
                  subject-path: |
                      main.js
                      manifest.json
                      styles.css

            - name: Extract changelog section for this version
              run: node scripts/extract-changelog.mjs "${{ github.ref_name }}" > /tmp/release-notes.md

            - name: Create release
              # v3 only moves the action's own runtime from Node 20 to Node 24
              # (its release notes say so, and its manifest declares
              # using: "node24"); the four inputs below are unchanged. v2 still
              # worked, but GitHub was already forcing it onto Node 24 and
              # warning on every release run.
              uses: softprops/action-gh-release@v3
              with:
                  tag_name: ${{ github.ref_name }}
                  name: ${{ github.ref_name }}
                  body_path: /tmp/release-notes.md
                  files: |
                      main.js
                      manifest.json
                      styles.css
